{
  "name": "zero-loss-adjustment",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "node --test",
    "check": "npm run lint && npm test && npm run build",
    "verify:prod": "node scripts/verify-production-build.mjs",
    "test:e2e": "playwright test",
    "check:full": "npm run check && npm run verify:prod && npm run test:e2e"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@playwright/test": "^1.61.1",
    "eslint": "^9.32.0",
    "globals": "^16.3.0",
    "vite": "^6.0.0"
  }
}
